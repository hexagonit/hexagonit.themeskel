<layers>
  <layer
      name="${namespace_package}.${package}"
      interface="${namespace_package}.${package}.browser.interfaces.I${namespace_package.title()}${package.title()}Layer"
      />
</layers>
